# Attribution

Sample records are attributed individually in the database and exposed by
`overseer sources` and `overseer evidence`. The source is the
[Independent Fallout Wiki](https://fallout.wiki/), whose contributors and
revision histories provide the underlying community reference material.

The exact page title, URL, page ID, revision ID, revision timestamp, retrieval
timestamp, and live license metadata are stored with imported records.

