# Migrations

Executable migration resources are packaged under `src/overseer/schemas/` so a
wheel installation can initialize its database without access to the checkout.
Migration design and review documentation remains anchored by this directory.

