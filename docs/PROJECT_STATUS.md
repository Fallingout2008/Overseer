# Project status

**Current state:** Phase 2 paused pending external source authorization.

Phase 1 remains preserved at commit `2e7ce54`. Its local vertical-slice database,
source cache, probe evidence, schema, manifests, and application code must not be
changed as part of this pause.

## Blocking condition

The Independent Fallout Wiki's `robots.txt` disallows `/api.php` for the
wildcard user agent. No published operator exception or approved official dump
path has been established for OVERSEER. The detailed evidence and acceptable
resolution paths are recorded in
[SOURCE_POLICY_BLOCKER.md](SOURCE_POLICY_BLOCKER.md).

While paused:

- do not bypass or reinterpret the `robots.txt` restriction;
- do not use a different host or endpoint to evade the restriction;
- do not use Fandom as a substitute;
- do not acquire additional lore or expand the retained sample;
- do not begin Phase 3 or any later phase.

Phase 2 may resume only after an authorized Independent Fallout Wiki operator
approves the proposed API use, or an official API/dump/export path with suitable
access and redistribution terms is identified and reviewed. Resumption also
requires explicit user direction based on that evidence.

## Roadmap state

1. **Phase 1 — Foundation and New Vegas vertical slice:** implementation
   complete; source-policy review flag remains intentionally open.
2. **Phase 2 — Full New Vegas and DLC archive:** paused before catalog discovery
   and bulk acquisition.
3. **Phase 3 and later:** not started and not authorized while Phase 2 is paused.

